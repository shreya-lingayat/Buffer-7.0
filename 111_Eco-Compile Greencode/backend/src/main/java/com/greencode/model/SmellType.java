package com.greencode.model;

public enum SmellType {
    NESTED_LOOP,
    LOOP_INVARIANT,
    BUSY_WAIT,
    STRING_CONCAT_IN_LOOP,
    PRIMITIVE_BOXING,
    UNCLOSED_RESOURCE,
    LINKEDLIST_USAGE,
    LEGACY_SYNCHRONIZED,
    MAP_EFFICIENCY
}
